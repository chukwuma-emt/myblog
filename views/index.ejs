<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Welcome | Blog by Chukwuma</title>

  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>

<div class="page">

  <!-- HERO -->
  <section class="hero">
    <div class="hero-content">
      <h1>Welcome to My Blog</h1>
      <p>
        Explore insightful articles, videos, and audio content on web development,
        tech trends, and personal experiences.
      </p>
    </div>
  </section>

  <!-- ARTICLES -->
  <section class="articles">
    <h2 class="articles__heading">Latest Posts</h2>

    <% if (data.length === 0) { %>
      <p>No posts available yet.</p>
    <% } %>

    <div class="articles-cards">
      <% data.forEach(post => { %>
        <div class="card">

          <!-- MEDIA -->
          <div class="media-box">
            <% if (post.mediaType === 'image') { %>
              <img src="/uploads/images/<%= post.mediaFile %>" alt="">
            <% } else if (post.mediaType === 'video') { %>
              <video controls playsinline>
                <source src="/uploads/videos/<%= post.mediaFile %>">
              </video>
            <% } else if (post.mediaType === 'audio') { %>
              <audio controls>
                <source src="/uploads/audio/<%= post.mediaFile %>">
              </audio>
            <% } else { %>
              <img src="/img/default-post.jpg" alt="">
            <% } %>
          </div>

          <!-- CLICKABLE CONTENT -->
          <div class="card-content" data-expand>

            <h2 class="post-title"><%= post.title %></h2>

            <p class="excerpt">
              <%= post.body.replace(/[#_*`>-]/g, '').substring(0, 80) %>...
            </p>

            <!-- FULL CONTENT -->
            <div class="full-content">
              <%- post.bodyHTML %>
            </div>

            <p class="article-list_date">
              <%= new Date(post.createdAt).toDateString() %>
            </p>

            <div class="post-meta">
              <small>👁️ <%= post.views || 0 %></small>
              <small>❤️ <%= post.likes || 0 %></small>
            </div>

            <p class="open-link">
              <a href="/post/<%= post.slug %>">Open full page →</a>
            </p>

          </div>

        </div>
      <% }) %>
    </div>

    <% if (nextPage !== null) { %>
      <a href="/?page=<%= nextPage %>" class="pagination">
        ← View Older Posts
      </a>
    <% } %>

  </section>

</div>

<script src="/js/main.js" defer></script>

</body>
</html>